# Module 6 — Agentic RAG Testing

# Core
python-dotenv>=1.0.0
rich>=13.0.0

# LLM client — the real planner/retriever/generator in examples/agent.py all run
# through this (chat completions + embeddings), same client Module 5 uses.
openai>=1.30.0

# Structured-output client for the LLM planner (examples/agent.py) — same
# instructor pattern Module 5's RAGAS judge setup uses.
instructor>=1.0.0

# Retry wrapper around embedding calls (transient Azure AI Foundry gateway
# errors) — already a transitive dependency of instructor, listed explicitly
# since agent.py imports it directly.
tenacity>=8.0.0

# Tracing — same LangSmith pattern from Module 5 Day 3, now tracing multi-hop chains
langsmith>=0.1.80

# Forward-looking (installed here so Module 7 has them ready)
# deepeval>=1.4.0       # uncomment when starting Module 7's agent metrics
