# Module 7 — AI Agents Testing with DeepEval

# Core
python-dotenv>=1.0.0
rich>=13.0.0

# LLM client — the tool-calling agent in examples/agent_tools.py runs through
# this (chat completions with function-calling/tools parameter), same client
# pattern as Modules 5 and 6.
openai>=1.30.0

# Structured-output client for judge calls — same instructor pattern as
# Module 6's agent.py.
instructor>=1.0.0

# Retry wrapper for transient API errors — already a transitive dependency of
# instructor; listed explicitly since agent_tools.py imports it directly.
tenacity>=8.0.0

# Tracing — same LangSmith @traceable pattern from Modules 5-6, now tracing
# individual tool calls; each decorated tool appears as its own child span.
langsmith>=0.1.80

# DeepEval — the formal agent metrics this module replaces Module 6's
# hand-rolled verdict functions with.
# TaskCompletionMetric, ToolCorrectnessMetric, ArgumentCorrectnessMetric,
# StepEfficiencyMetric, LLMTestCase, ToolCall
deepeval>=1.4.0
