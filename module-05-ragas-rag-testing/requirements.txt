# Module 5 — RAG Testing with RAGAS

# Core
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-html>=4.1.0
rich>=13.0.0

# LLM clients
openai>=1.30.0

# RAGAS — notebooks use its native llm_factory/OpenAIEmbeddings (no LangChain
# wrapping needed for the judge LLM/embeddings).
# langchain-community is still a transitive ragas dependency: ragas/llms/base.py
# unconditionally imports langchain_community.chat_models.vertexai, a module that
# 0.4.0 removed — capped <0.4 so `import ragas` itself doesn't break.
langchain-community>=0.3.0,<0.4
datasets>=2.19.0
ragas>=0.2.0

# Structured-output client for the RAGAS judge LLM (ragas.llms.InstructorLLM) --
# used by examples/01_ragas_intro.ipynb and ci/judge_model.py.
instructor>=1.0.0

# Retry wrapper around ci/judge_model.py's embedding calls (transient Azure AI
# Foundry gateway errors) -- already a transitive dependency of instructor, listed
# explicitly here since ci/judge_model.py imports it directly.
tenacity>=8.0.0

# LangSmith tracing (Day 3) — observe retrieval + generation steps for debugging failures
langsmith>=0.1.80

# Lightweight local vector store + embeddings for the chunking/retrieval demos
# (keeps Day 2 runnable without a real OpenAI key — falls back to local embeddings)
scikit-learn>=1.4.0
numpy>=1.26.0

# Forward-looking (installed here so Module 6 has them ready)
# chromadb>=0.5.0       # uncomment when starting Module 6's vector store work
